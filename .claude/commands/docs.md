Run `scripts/docs-list` to see all available docs with summaries and "read when" hints.

This is MANDATORY before starting any task. If any doc's "Read when" matches your current task, read that doc NOW.

Also read the `CLAUDE.md` in the project subfolder you're working in (e.g. `nhl-betting/CLAUDE.md`).
